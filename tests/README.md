# Backend checks

Run from the outer `Trusttrap-main` folder:

```powershell
python -m unittest discover -s tests -v
```

These tests cover the deterministic, privacy-preserving text and URL report
assembly. Model quality still needs a consented evaluation set from real users.
